services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: portal
      POSTGRES_PASSWORD: portal
      POSTGRES_DB: portal_utzmg
    ports:
      - "5432:5432"
    volumes:
      - portal_pg_data:/var/lib/postgresql/data

volumes:
  portal_pg_data:
